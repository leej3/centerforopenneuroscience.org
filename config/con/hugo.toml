# CON environment overrides. The upstream `_default` configuration remains
# the shared base and is merged by `hugo --environment con`.
baseURL = "https://centerforopenneuroscience.org/"
summaryLength = 30
disableKinds = ["RSS"]

[outputs]
  home = ["HTML"]
